
// Say function
function Say(message) {
   print("Say function in HelloWorld.js " + message);
}


function addNumbersJs(a, b) {
   return a + b;
}

function addNumbersJavaThroughJs(a, b) {
	return ConstantsTest.addNumbersJavaThroughJs(a, b);
}

function Say(msg) {
	a = "Say" + c;
	print(a + msg);
	com.stottlerhenke.simbionic.test.engine.TestEngine.buffer.append(msg);
}

function put(key, value) {
	print("Put: " + key + " => " + value);
	com.stottlerhenke.simbionic.test.constants.ConstantsTest.map.put(key, value);
}


